// components/tabs/Bars.tsx
import { View } from 'react-native';
import { Text } from 'tamagui';

export default function Cafes() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text fontSize="$4">🍻 Cafes</Text>
    </View>
  );
}
